name: CI/CD Demo

on:
  push:
    branches: [ "main" ]

jobs:
  pipeline:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build
      run: echo "🔨 Building project..."

- name: Fake Test
  run: |
    echo "🧪 Running fake tests..."
    echo "❌ Test failed!"
    exit 1

    - name: Deploy
      run: echo "🚀 Deploying..."
